[project]
name = "codefaith"
version = "0.1.0"
description = "description"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ast-grep-py>=0.39",
]

[project.optional-dependencies]
llm = [
    "openai>=1.0",
]
